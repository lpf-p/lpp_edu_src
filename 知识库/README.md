# 知识库索引

实战方法论 / 测试清单 / 场景矩阵。与 `SKILL.md` 流程配合使用。

## 使用约定

- 进站先读 `打穿短表.md`；对得上再打开对应模块看细节。文件不长就整篇开；超长篇可先开点名节，不够就继续开。禁止每站通读本目录
- 磁盘有 `*src经验.md` 才开专篇，没有不算缺。开 `SKILL.md` 不会再带集团日记
- 短表和「注入/SSRF/XSS/RCE」都不是上限。本站过全类型矩阵；四件套打在有差分面上（防空窗），不是只测这四类，也不是每个 path 喷 `'`。有会话时越权/逻辑与四件套同硬（`dig-scope` §4.2.3）
- 方便和能力优先；省 token 是顺带，不挡开模块
- 短表点名的手法用标题搜。有指针的肥篇只留实战中文 + 指针段；禁开/几乎不交的篇已收成一行。手法行不删、算成不改矮。
- 篇内跳转已改成本目录真实文件（`idor-test.md` 一类）；不要再跟 `../xxx/SKILL.md`
- 与 `rules` 冲突时 **以 rules 为准**（挖什么 `src-value`；CORS 不挖 `cors-vuln-report-priority`；写不写 `vuln-report-format`）
- **`cors-test.md` / `llm-security-test.md`：不挖/禁开越狱。** `401-403-bypass.md` 不磨登录 HTML。
- 正式 SRC 报告：`rules/vuln-report-format.md`

## 文件清单

| 文件 | 说明 |
|------|------|
| `打穿短表.md` | 挖洞手法索引（一行/指针；正文仍在各模块） |
| `401-403-bypass.md` | **禁开磨登录 HTML**（已收成一行）；业务 API 401 现场自己打 |
| `api-gateway-test.md` | API 网关 |
| `artifact-intel-guide.md` | **前端产物情报提取**：Service Worker / lock 文件 / `.env` / CI 配置 / **目录列目录 autoindex（含探测字典）** / **文档与表格泄露（使用手册/初始密码/名单）** / 元数据 / GraphQL introspection / 同类面家族清单 / 去伪（SPA catch-all） |
| `agent-tool-exec-test.md` | 对话口工具真执行（不是越狱、不是云 IDE RPC） |
| `authbypass-test.md` | 认证绕过（未登录改密/IDaaS + 短表指针；英文字典已砍） |
| `cache-poisoning-test.md` | 缓存投毒/欺骗（原有+补充） |
| `clickjacking-test.md` | 缺头不写（已收成一行） |
| `cloud-ide-codex-rce-chain.md` | 云 IDE/Codex 系：弱口令→RPC RCE→集群/API Key 链（短表有指针） |
| `cors-test.md` | **不挖勿开**（已收成一行） |
| `crlf-injection-test.md` | 几乎不交（已收成一行） |
| `csp-bypass-test.md` | 几乎不交（已收成一行）；XSS 走 `xss-test.md` |
| `csrf-test.md` | 专题知识（hack-skills 导入或融合） |
| `csv-formula-injection-test.md` | 几乎不交（已收成一行） |
| `dangling-markup-test.md` | 几乎不交（已收成一行） |
| `dependency-confusion-test.md` | 几乎不交（已收成一行） |
| `deserialization-test.md` | 专题知识（hack-skills 导入或融合） |
| `dns-rebinding-test.md` | 几乎不交（已收成一行）；SSRF 走 `ssrf-test.md` |
| `el-injection-test.md` | 专题知识（hack-skills 导入或融合） |
| `email-header-injection-test.md` | 几乎不交（已收成一行） |
| `file-upload-test.md` | 文件上传（STS/列桶/分享鉴权等指针） |
| `ghost-bits-cast-test.md` | Ghost Bits 原理+常用字+公式；逐字节两套表已砍 |
| `graphql-test.md` | GraphQL（原有+补充） |
| `hpp-test.md` | 几乎不交（已收成一行） |
| `http-host-header-test.md` | 专题知识（hack-skills 导入或融合） |
| `http-smuggling-test.md` | 请求走私（原有+补充） |
| `http2-attacks-test.md` | 几乎不交（已收成一行）；走私走 `http-smuggling-test.md` |
| `idor-test.md` | 越权（中文主线 + 短表指针）**+ 业务接口未授权**：核心认知（越权=缺失的艺术，只能靠差分发现）/ 五大根因 / 判定三条件 / 多账号矩阵 / 改·加·删·替换四动作 / ID 枚举三法 / Autorize·AuthMatrix / 前端文案线索 / 组合利用四链 / 白盒审计模式 / 未授权接口清单五来源 + 四个高发位置 + 去伪 |
| `info-leak-test.md` | 信息泄露 |
| `injection-test.md` | 注入（OR+total / 邮件订阅 iframe / SSTI 探测；英文百科已砍） |
| `insecure-scm-test.md` | 专题知识（hack-skills 导入或融合） |
| `jndi-injection-test.md` | 专题知识（hack-skills 导入或融合） |
| `js-reverse-guide.md` | JS 逆向：接口发现 + 签名复现 + **sourcemap 还原源码** + **硬编码凭据正则清单** + 隐藏路由/影子 API |
| `llm-security-test.md` | **禁开越狱教材**（已收成一行）；对话工具走 `agent-tool-exec-test.md` |
| `logic-test.md` | 业务逻辑（支付/流程 + 商家促销绑定 + **数值测试矩阵**（0/小数/负数/正负组合/整数溢出）+ **支付签名缺失/订单替换/传输层降级(https→http)** + **状态机/步骤跳过通用绕过** + 越权/绕过参数清单 + **按业务场景发散清单**（电商/支付/外卖/直播/社交/漫画/网约车/活动/会员十大场景））|
| `middleware-unauth-test.md` | 中间件/框架未授权面（Nacos/Actuator/Swagger/ES/Kibana…）+ 影子 API |
| `oauth-jwt-test.md` | OAuth/JWT/SAML/OIDC（原有+多源补充） |
| `open-redirect-test.md` | 专题知识（hack-skills 导入或融合） |
| `password-reset-test.md` | **密码重置/找回/申诉全链**（验证码零校验、身份字段逐个独立性、token 熵、佐证合并、步骤跳过、**响应包改状态值**（verified/showResetPassword 后端不二次校验）、**接收端参数污染+步骤间身份不一致**）|
| `path-traversal-lfi-test.md` | 专题知识（hack-skills 导入或融合） |
| `postmessage-dom-test.md` | 客户端：postMessage / DOM Clobbering / 跨窗口泄漏 |
| `prototype-pollution-test.md` | 专题知识（hack-skills 导入或融合） |
| `race-condition-test.md` | 竞态（原有+补充） |
| `ratelimit-abuse-test.md` | 接口滥用：限流绕过 / 验证码绕过 / 轰炸 / 资源耗尽 / 薅羊毛 |
| `recon-methodology.md` | 侦察方法论 |
| `recon-fingerprint-cdn-wildcard.md` | **进站三件事**：指纹识别（国产 CMS/框架→打法映射 + favicon hash 反查）、CDN 判定与源站穿透、泛解析判定与子域两层过滤 |
| `edge-asset-hunting.md` | **偏站/老站/边缘资产挖掘**（出洞率最高）：7 类来源清单 + 筛法 + 老站高频薄弱点（弱口令/全局未授权/仅前端校验/Nday/未授权服务）+ §0 红线（不社工不钓鱼） |
| `login-bypass-playbook.md` | **登录框十枪**（通用手法）：删参数 / 改响应包 / 分包验证 / 验证码 6 种缺陷 / 默认凭据 / 万能密码 / 目录爆破 / 枚举差分 / 状态码 / JWT。与 `authbypass-test.md`（具体系统案例）互补 |
| `nday-watchlist-2026.md` | **2026 高危 Nday 速查**：泛微 E-cology 未认证 RCE、调度面板大小写绕过、OpenAM 反序列化、Tomcat 修复回归、Next.js SSRF、NGINX rewrite、FastAPI BadHost、Ghost CMS… 按国内 SRC 可挖度排序 |
| `redirect-ear-unauth.md` | **3xx 截断跳转 / CWE-698 重定向后继续执行**：原理（PHP `header` 不 `exit`、Java `sendRedirect` 不 `return`）+ 状态码决定性判定（3xx vs 200）+ 三档后果（信息泄露/未授权读/未授权写）+ 鉴权 path 匹配不全 |
| `sqli-advanced-test.md` | **SQL 注入进阶**：布尔盲注 / 时间盲注 / 带外 OOB / 二次注入 / 堆叠 / 写文件 |
| `ssrf-test.md` | SSRF（IMDS 路径差 / GOPROXY / 对象存储回源） |
| `subdomain-takeover-test.md` | 专题知识（hack-skills 导入或融合） |
| `type-juggling-test.md` | 专题知识（hack-skills 导入或融合） |
| `waf-bypass.md` | WAF 绕过 |
| `websocket-test.md` | WebSocket（原有+补充） |
| `xslt-injection-test.md` | 几乎不交（已收成一行） |
| `xss-test.md` | XSS（中文开场 + 冷门事件 + XSS→RCE / 自定义协议） |
| `xxe-test.md` | 专题知识（hack-skills 导入或融合） |
| `captcha-bypass-test.md` | 滑块/行为验证绕过：服务端缺陷能交（响应包改 success、token 可伪造复用、只校验调过接口、换入口），逆向第三方 SDK 超范围不交 |
| `biometric-bypass-test.md` | 生物特征/活体/实名认证绕过：服务端真复核活体结果 + 人脸真绑定账号（改响应 live、token 不绑会话、绑别人 user_id），只用自己账号测不冒充他人 |
| `src-case-study.md` | SRC 案例复盘与报告写法：挖洞全流程 + 状态篡改绕过审核案例 + 七字段报告模板 + 洞链串联 |
| `src-platform-and-search.md` | **SRC 平台目录 + 搜索技巧**：国内企业自建 SRC / 众测平台（补天·漏洞盒子·火线·360众包）/ **EDUSRC 教育（`src.sjtu.edu.cn`，上海体育大学属此）** / 国际（HackerOne·Bugcrowd）/ 社区（先知·安全客·FreeBuf）/ 官方漏洞库（CNVD·CNNVD·CNCERT）+ Google dork / GitHub dorking / 组合关键词 |
| `ima-case-corpus.md` | **ima 实战案例库提炼**（2026-09-14 补入，源自 ima `src` 知识库 1300+ 份真实报告去重）：EduSRC 专供打法（信息收集→验证码回显接管）、企业高频场景（收货地址越权/限制购买/OAuth 绑定劫持/小程序云上传）、通用系统指纹×POC 速查表、无回显 SSRF 时长判活、`/:` 注释拼接 XSS 绕过、报告骨架模板。要看报告原文用 ima 连接器搜知识库 `src`（结构 `src报告/<漏洞类型>/<平台>/`） |
| `logic-web-cases.md` | **逻辑漏洞 Web 实战案例深挖**（2026-09-14 补入，ima `src报告/逻辑漏洞/Web` 195 条去重后 **88 份全量精读**）：8 大类 32 种打法 + **按功能点的通用排查 Checklist**（注册登录/找回密码/改绑定/支付下单/活动领取/后台权限/资源文件/资源滥用）+ 88 份案例索引表 + **厂商定级尺度实录**（并发领券定严重、并发书架中危 300 元、代金券并发中危 200 元、验证码篡改低危 50R）+ 写报告三个提分点。含支付 11 类打法、Turbo Intruder `race.py` 标准流程与并发失败 4 原因、验证码 8 种姿势与绕频控矩阵、密码重置 5 类成因 |
| `ima-retrieval-index.md` | **章节 → ima 检索式回查表**（2026-09-14 补入，P4）：本库 18 个案例文件均由 ima `src` 深挖压缩，**原文不在本地**；本表给出 **13 个类型目录 `folder_id`** + 各文件「章节 → 检索关键词」+ 三条回查路径（**报告名直搜** > 关键词 > 系统/厂商名）+ 副本判重提醒 + `get_knowledge_list`/`fetch_media_content` 调用模板。需要回看原始报告时先查此表 |
| `ima-corpus-progress.md` | **案例库深挖进度与待补充清单**（2026-09-14 补入）：ima `src报告` 全库 **1604 份**家底表（13 类 × Web/EduSRC/App/小程序）+ 分级待办（P0 收口逻辑漏洞剩余 27 份 / P1「其他」762 与越权 168 / P2 小体量十类打包 / P3 EduSRC 337 与移动端 37 跨类专项 / P4 知识文件增强）+ 批次估算 + **可复用深挖工作流六步与已知坑**。要继续深挖或查进度时先看这份 |
| `edusrc-cases.md` | **EduSRC 教育行业专项（按业务场景组织，非按漏洞类型）**（2026-09-14 补入）：当前含**逻辑漏洞 15 案**（原始 27 条去重后 21 份唯一文件全量精读）——epay `?method=*FindList/resetPwd/Update` 未授权拖库+改密+提权（一套系统多校复用）、yn智慧校园未授权拖师生库、弱校验找回密码任意改密（清华/川大）、ASP.NET `user_id` IDOR 改密（上海交大/浙大）、Cookie `webUserName` 水平越权、返回包 `role` 与 `hasLocalAccount` 篡改、4 位验证码 Intruder 全空间爆破、短信轰炸、南软研究生系统 base64 绕 WAF getshell。**§2.8 越权专项**（越权 EduSRC 47 条 → 42 份唯一 → 34 有效新案例）：就业网 `?Xsxh=` 遍历（多校同源码）、`getObjList` 改学号、`insuranceApplicationId` 自增遍历、swagger 未授权含明文密码、响应包 `status` 绕登录后导出（兰大 HR/萃英学院）、`switchPosition` 提权、采购系统拖 2764 份证件照、仿真平台泄露库凭据、同济 8 个越权点。附 **EduSRC 场景总纲**（公示 xlsx 拿学号是胜负手）+ **指纹×打法速查表**（含 §2.9~§2.13 新增的 13 条校园系统指纹）+ 教育行业 Checklist + 定级观察。**§2.9~§2.13（2026-09-14 补齐）**：校园 getshell 与上传 12 案（正方/强智/南软 V5.0/先极/微宏 OA/东方仿真，含三种别的报告没有的 WAF 绕过：`asmx`+空格+换行拆词、`/wh/;/../`、后端自动删 `../`）→ 注入类 9 案（→ `xp_cmdshell` 写马/RCE）→ 信息泄露 6 案（陕西师范 4.6 万条 **严重(8)**、actuator heapdump + Nacos）→ 认证与口令 5 案（SOGo `system:system`、同济云媒体 `admin:admin888`、浙大 phpMyAdmin 空密码）→ 其他类型 4 案（附件改名 html 的存储 XSS、同济 `.do` 改密 CSRF 两次定级差异、兰大 jsoa SSRF、WordPress `xmlrpc.php` XXE）。**至此 EduSRC 教育侧已深挖的类型全部闭环** |
| `idor-cases.md` | **越权 / 未授权实战案例深挖**（2026-09-14 补入，ima `src报告/越权/` 168 条去重后 **112 份**精读：Web 70 + EduSRC 42）：8 大类企业实战打法（**对象 ID 替换**（订单/单据/资产/媒体 id 遍历）/ **用户标识参数替换**（`userId`/`unionId`/`loginUuid`/`memberId`/`fromAccid` 顶号与接管）/ **响应包篡改**（`login`/`status`/`SignId`/布尔权限位/`userInfo` 替换）/ **Cookie·token 缺陷**（MD5 结构重构、Base64 token 遍历）/ **删参改排序退化全表**（兰州大学删 `jsbld`/`xqcode` 改 `sort=id.asc`）/ **路径替换·直访受限 URL·废包**（`/cardInfo/`→`/cardPersonal/`、vlab 改名登老师、丁香园丢校验包）/ **客户端鉴权**（JS 只校验用户名、隐藏字段 `*userid`）/ **垂直越权** / **未授权访问**（lx-files、SPA `#/` 直访、调度平台 csv）/ **任意密码重置 9 类** / **多租户通杀**）+ 教程真增量（XFF 绕 403、验证码参数名替换、17 种组件未授权、K8s 未授权链、后台路径+fuzz）+ **CNVD 设备指纹速查表**（锐捷/HP/共济/网心云/H3C/科立讯）+ 案例索引表 + **麦当劳 SRC 定级尺度实录**（高危 +40 积分 vs 供应商资产拒收）+ 素材缺口。**教育侧越权已并入 `edusrc-cases.md` §2.8** |
| `other-census.md` | **「其他」762 份构成调研**（2026-09-14 补入，P1-4 交付物）：分格拉全标题 + 归类 + 18 份抽样精读，回答"「其他」到底装了什么"。**核心结论**：「其他」不是漏洞类型而是**未归位附件倾倒口**（Web 547 = 企业单位报告 131 + 漏洞专题 115 + 科普系列 102 + SRC 方法论 57 + 杂项 49 + 实战攻防 40 + HW 18 + 电子书 16 + 资产工具 11 + 制度 8；EduSRC 185 = 教育行业历史提交归档，集中在弱口令 + 校园系统 Nday；App/小程序 = 移动端方法链路）。去重后 ≈562 份，**不宜全量精读**，建议**三分法**（可挖原料按主题打包只抽新点 / 只归档不精读 / 副本丢弃）。含 6 条完整攻防链（EDR 二次认证文件删除绕过、域渗透 dcsync、DNS 隧道、AC Host 白名单绕过、泛微三层 URL 编码、K8s etcd→Pod 逃逸）+ 安卓加固对抗（改 SM4 key 表）+ 小程序三件套与 bucket 接管。原始清单在 `_work/other-web-census.md`、`_work/other-edu-app-census.md` |
| `attack-chain-cases.md` | **实战攻击链案例深挖**（2026-09-14 补入，第 2a 批首件）：从「其他/Web」筛出**内容体裁为"完整攻击链复盘"的 36 份**（35/36 读取成功），**按攻击链阶段重组**而非按报告罗列——① 六阶段总纲（边界突破→首次权限→上线→提权→横向→域/云/靶标）与各阶段"最稳动作"；② 目标选取与资产测绘手法清单（hunter `icp.name`、C 段统计脚本、IP 上下浮动、APIKit/HaE 被动、`.ds_store`+DS_Walk）；③ **弱口令字典构造规则与高价值命中场景**（登录页泄工号、系统间撞库、姓名转拼音、OA 流程翻 VPN 初始密码）；④ **文件上传绕 WAF 全景表**（双 `Content-Disposition`+`form-datA`+`$20` / `filename;;;;`+`.cer` / `.png.asp` 双后缀（`IndexOf` 判整个文件名）/ `Content-Encoding` 绕内容检测 / Tomcat WAR `filename=".w\a\r"` / ueditor catchimage / ewebeditor / 假白名单）；⑤ **国产 OA·中间件 Nday 速查**（泛微 browser.jsp **三层 URL 编码** + 手开 xp_cmdshell、致远/泛微/o2oa 默认口令、O2OA jaxrs 脚本引擎 RCE、用友 NC `prop.xml`、DTcms 读 web.config、帝国 CMS 五条 getshell 链、Shiro 默认 key 与 Yso 重编译、CVE-2022-22947、Nacos token 伪造）；⑥ **不出网/站库分离落地**（certutil `-encode/-decode` 分段、**AC 设备 Host 白名单绕过**（CS listener Host 改官网域名）、Neo-reGeorg 404 模板隐藏隧道、中文目录借 bat 写马、rportfwd/frpc 中转）+ **站库分离判定四步**；⑦ EDR/杀软对抗（**删 `sfrdpverify.exe` 绕 EDR RDP 二次认证**、深信服 `sys_account.json`、蓝队 `web.config` 禁脚本→用任意文件删除反制）；⑧ 提权与凭据收集（土豆家族、**MSSQL `xp_cmdshell` 兜底**、phpMyAdmin `general_log`、MS17-010 `ms17_010_command`、`dcsync` 导全域哈希、**strace 抓 sshd 明文密码**、**ToDesk `config.ini` 替换 `tempAuthPassEx` 还原临时密码**、浏览器/Xshell/QQ 目录/云盘密码本）；⑨ 横向与集权收尾（`mstsc /admin` 绕许可、`clone.exe` 克隆桌面、Host 头碰撞找内网靶标、域控 Zerologon、**堡垒机齐治/中远麒麟/优炫/帕拉迪 SQL 改密**、vCenter 41433 MSSQL 口令碰撞、**etcd→SA token→`hostPath:/`→`chroot` Pod 逃逸**、云 AK/SK + cloudTools 接管）；⑩ 排查 Checklist + 36 份案例索引表 + 战果/定级观察 + 素材缺口与扩展位。**§0 使用红线**：其中社工/钓鱼/水坑/伪造发件人、免杀对抗、卸载 EDR 部分**仅作威胁认知，SRC 场景一律不做**（对齐 `edge-asset-hunting.md` §0）。中间产物 `_work/unique-list-chain.md` + `deep-chain-a~c.md` |

| `sqli-cases.md` | **SQL 注入实战案例深挖**（2026-09-14，P2 批）：ima `src报告/SQL注入/` 97 条 → 去重 **72 份唯一**，实读 18 份。**按数据库类型与注入点重组**（MySQL / MSSQL / Oracle / 小程序 json / 排序参数 / JSON·Referer 头 / cookie 参数），含真实 payload：`exp(720)` 溢出报错判库、`sort/exp(824-ascii(SUBSTRING(user(),1,1)))` 逐位爆破、`ORD(MID(@@hostname,1,1))` 布尔盲注、WAF 禁常用函数时改用 `exp(0)=1`／`DBMS_PIPE.RECEIVE_MESSAGE`／`utl_inaddr.get_host_address`。附 WAF 绕过 payload 表 + 排查 Checklist + 案例索引 + 定级观察 |
| `xss-cases.md` | **XSS 实战案例深挖**（2026-09-14，P2 批）：ima `src报告/XSS/` 96 条 → 去重 **51 份唯一**，实读 30 份（29 成功 + 1 失败）。按类型重组（反射/存储/DOM/盲打/富文本与编辑器/文件类 XSS/CSP 绕过），含 WAF 过滤逐层试探法（先判过滤的是符号/标签/属性/关键字）、self-xss 配 CSRF 放大、mXSS 与 UBB 特殊跨站、xss platform / beef 盲打。**⚠️ 素材中 2 份含真实账号与个人隐私，仅保留技术点（上传改名 html）并标红线不落地** |
| `infoleak-cases.md` | **信息泄露实战案例深挖**（2026-09-14，P2 批）：ima `src报告/信息泄露/` 75 条 → 去重 **44 份唯一**，实读 26 份。**按泄露载体重组**（配置文件 / 调试端点 actuator·heapdump / 源码仓库 / 备份与临时文件 / 云存储 AK·SK / 前端 JS 与 sourcemap / 日志与报错 / 数据库端口 / 目录列举），真实路径如 `build.xml` 拿 SSH 部署账号、用友 NC `/portal/docctr/open/word.docx?disp=/WEB-INF/web.xml` 读 `web.xml`、芋道 `file-config/page` 未鉴权返回 OSS `accessKey`/`accessSecret`。含**高价值泄露点速查表** + Checklist + 定级（哪些只能算低危、哪些能升高危）。全部个人数据只描述类型不抄录 |
| `cmd-injection-cases.md` | **命令注入 / 服务端直接执行案例库**（2026-09-14，P2 批）：ima `src报告/命令注入/` 49 条 → 去重 **38 份唯一**，精读约 25 份。**核心是「组件指纹速查表」**（Struts2 / Shiro / Fastjson / Log4j / ThinkPHP / 泛微 / 致远 / WebLogic / Jenkins / Solr / Confluence / Nacos / Spring / XXL-JOB 等：漏洞点路径 → payload → 回显或外带方式 → 来源案例）+ 打法分类（命令拼接 / 代码执行 / 反序列化 / SSTI / 表达式注入 / 未授权 RCE）+ 不出网无回显落地（DNSLog·ICMP·时间盲·写文件·转发）。红线：只验证无害回显，不落持久化、不碰业务数据 |
| `csrf-cases.md` | **CSRF 实战案例深挖**（2026-09-14，P2 批）：ima `src报告/CSRF/` 43 条 → 去重 **35 份唯一**（含 7 份截图证据），实读 21 份。按类型重组（GET / POST 表单 / JSON / 文件上传 / 登录登出 / CORS 组合 / 多步操作链）+ **PoC 模板库**（可直接复用的 HTML/JS，每段 ≤8 行）+ 防护缺失归类（无 token / token 可预测 / 未绑定用户 / Referer 可绕 / SameSite 缺失 / 仅前端校验）。**重点章节：怎么论证危害把 CSRF 从低危推上中危** |
| `file-upload-cases.md` | **文件上传实战案例深挖**（2026-09-14，P2 批）：ima `src报告/文件上传/` 40 条 → 去重 **32 份唯一**，实读 18 份。**按防护层级重组**（前端 JS / MIME 与 Content-Type / 后缀黑名单 / 后缀白名单 / 内容检测与 WAF / 二次渲染 / 解析配置与别名映射 / 上传路径可控），含**组件与编辑器上传点速查表**（UEditor / ewebeditor / kindeditor / fckeditor / 帝国 / Tomcat / Weblogic / 致远 / 泛微 / 通达 / 宝塔）+ 上传后利用链（拿路径→解析→getshell→提权/跨目录/覆盖配置/计划任务）。红线：止于无害证明，不落地后门 |
| `weak-password-cases.md` | **弱口令与默认口令实战案例**（2026-09-14，P2 批）：ima `src报告/弱口令/` 25 条 → 去重 **20 份唯一**，实读 19 份。**核心是「系统指纹 × 默认口令速查表」**（国产 OA 致远/泛微/通达/蓝凌、中间件 Tomcat/Weblogic/Nacos/Jenkins/Zabbix/Nexus、数据库、网络设备、安防设备、终端管理、堡垒机、校园系统 → 默认账号 / 默认口令 / 登录路径 / 命中条件）+ 撞库与喷洒手法（字典构造、锁定与验证码规避、并发限速、成败特征）+ **"等价初始态"**（身份证后六位 / 工号 / 手机号后四位 / 企业名+年份；首次登录强改密的绕过）+ 怎么论证危害升档。**不抄录任何真实可用凭据** |
| `ssrf-cases.md` | **SSRF 实战案例深挖**（2026-09-14，P2 批）：ima `src报告/SSRF/` 16 条 → 去重 **10 份唯一**，实读 10 份。**SSRF 参数点速查表**（业务场景 / 参数名 / 可用协议 / 能做什么）+ 绕过过滤手法汇总（黑名单、DNS rebinding、302、进制与短网址、`@` 与 `#` 混淆、gopher 打 Redis/MySQL/FastCGI、file/dict、IPv6 与八进制 IP）+ **云环境利用链**（`169.254.169.254` / `100.100.100.200` 各云差异 → 拿 AK/SK → 接管）+ 易出 SSRF 的功能点 Checklist（图片加载 / URL 预览 / webhook / 导出 / XML 解析 / 代理 / 抓取 / 截图） |
| `lfi-xxe-cases.md` | **文件包含与 XXE 实战案例**（2026-09-14，P2 批，小体量合并）：ima `src报告/文件包含/`（8 条）+ `src报告/XXE/`（3 条）→ 去重 **9 份唯一**，实读 11 份。LFI 部分：参数点速查表 + 利用链（读源码 → 找马路径 → 日志投毒/上传包含 → getshell，`php://filter` 与 `data://`）；XXE 部分：触发点速查表（XML 上传 / 接口 body / SOAP / Office 文档 / SVG / RSS / SAML）+ 无回显 OOB 外带 DTD。**共同注意点：两者常被判低危/信息泄露，如何论证升档** |
| `code-audit-cases.md` | **代码审计与漏洞专题实战案例**（2026-09-14，2a 批）：ima `src报告/其他/Web` 547 条中筛出"漏洞类型与代码审计专题"约 115 条 → 去重 **41 份唯一**，精读 **23 份**。**按语言/框架列审计切入点**：Java（MyBatis `${}` 拼接 → SQL 注入、SpEL、反序列化、权限注解缺失、不安全对象调用）、PHP（ThinkPHP/CI/Laravel、文件操作、变量覆盖、伪协议）、前端 JS 与 .NET；附**危险函数/危险关键字速查表**（搜什么 → 为什么危险 → 怎么利用）+ JWT / API 安全 / 文件覆盖 / 人脸识别绕过等专题实操。`CORS.pdf` 按技能红线不纳入 |
| `vendor-system-cases.md` | **国产 / 厂商系统漏洞速查表**（2026-09-14，2a 批）：ima `src报告/其他/Web` 中"企业与单位测试报告"约 131 条 → 去重后与 `logic-web-cases.md`/`idor-cases.md`/`attack-chain-cases.md`/各类型 cases **交叉查重**，只取新案例。**定位：认指纹 → 知道打什么**，按"产品/系统"组织（OA 类 / HR 人事 / 报表 BI / ERP 财务 / CMS / 中间件组件 / 国产安全设备 / 行业专用系统），每行给指纹特征、漏洞点与路径、payload、危害与定级、来源案例。附行业分类要点 + **重复提交与拒收经验**（哪些系统被反复提交、怎么提高接收率）。**2026-09-15 重挖补录 §七**（原判"其余 90 条为同手法副本"有误，实为全库唯一的网络设备与云原生素材）：实读 8 份，新增速查表 #31~#37 —— 网络设备 6 指纹（**锐捷 NBR 全系** `/index.data?opt=err` 未授权读全量配置、**锐捷睿易** `admin:admin` 进 LuCI、**网心云** 21.8 万资产 9999 免登录后台且可复位设备、**H3C ER6300** 日志+关验证码+全站 `goform/aspForm` 未授权三连、**宇视 ISC** `LogReport.php` 回写型 RCE、**安美 HiBOS2** `server_ping.php` 管道注入）+ **K8s API Server 8080/6443 未授权 → `hostPath` Pod → 写 SSH 公钥/crontab/chroot 接管宿主机完整链**。另附三条跨文件认知：**找贴牌固件不找品牌名**（锐捷NBR=WayOS、睿易=LuCI、H3C=Miniware-Webs）、**设备藏在非标端口**（9999/6060/5555/8088…只扫 80/443 全漏）、**嵌入式头号问题是功能端点不鉴权而非注入**
| `mobile-cases.md` | **移动端（App / 小程序 / 公众号）实战案例与打法**（2026-09-14，P3 批）：ima `其他/App` 20 + `其他/小程序` 10 + `其他/EduSRC` 移动端条目 + 各类型目录 App/小程序 子格 → 去重 **28 份唯一，实读 28 份，失败 0**。**技能包移动端从 0 到 1 的打法底座**：① 抓包链路（Android / iOS / PC 微信各版本、Proxifier 转发哪些进程、证书与双向认证、SSL Pinning 绕过、常见失败排查）；② 小程序逆向三件套（`__APP__.wxapkg` 解密与反编译、`WeChatOpenDevTools` 开 F12 的版本偏移、分包与插件包）；③ 加固与反调试对抗（加壳识别 → 脱壳 → native 定位 → 算法还原，**"改密钥表而非改算法"** 的省力思路）；④ 客户端校验绕过（改包重放 / 本地时间 / 前端金额与数量 / 抽奖概率 / 返回值篡改）；⑤ 移动端高价值场景（云存储 AK·SK 与 bucket 接管、未授权接口、OSS 直传、登录态与 token 缺陷、分享与授权链） |
| `archive-inventory.md` | **「其他」归档清单与副本对照（2b / 2c）**（2026-09-14）：762 条中**不精读但也不丢弃**的部分完整留档 + **副本删除对照依据**。2b 归档：安全科普与系列文章 102、HW 红蓝对抗 18、电子书与教程 16、制度合规 8、EduSRC 单点提交归档 56（表格含涉及学校与疑似漏洞类型）。2c 副本对照：① 明确副本（标题规整后相同且 size 相同 → 可删）；② **标题同但 size 不同（不可判重、必须各自保留）**；③ 图片与碎片 49（含"已被哪个案例覆盖"与处理建议）。全部表格可查、每条一行，media_id 已还原完整形式 |

| `ssti-test.md` | **SSTI 服务端模板注入（打深篇）**（2026-09-15 补入）：`injection-test.md` §SSTI 是总览与快速检测，本篇是打深。**先认「渲染口」**——邮件/短信模板、导出与生成 PDF、自定义页面与 CMS 主题、报表 BI、错误页/维护页、Webhook 模板、SSR（命中率从高到低）；一眼排除「只进数据库」「只做等值查询」「纯前端渲染（那是 CSTI/XSS）」。含**引擎识别**（四探针决策树 + `{{7*'7'}}` 多引擎区分 + **报错定栈表**：`jinja2.exceptions` / `Twig_Error_Syntax` / `freemarker.core` / `TemplateProcessingException` / `ActionView::Template::Error`… → 报错是白给的引擎名与版本号，直接查 Nday）+ 各栈 RCE 与沙箱逃逸（Jinja2 `attr()` 绕点号与 `request.args` 外带 payload、Twig `filter('system')`、FreeMarker `Execute`、Velocity `ClassTool`、Thymeleaf 预处理 `__${}__` 与"业务拼 viewName"、Pug/EJS/ERB/Smarty/Mako/Tornado/Razor）+ 无回显判定（DNSLog / 时间盲 / 写文件 / 纯表达式差分）+ 过滤绕过表。**判据：Handlebars / Go html/template / Liquid 打不出 RCE 是设计使然**，转信息泄露收手，别磨。红线：命令只做无害标记 / `id`。注：ima 案例库 1604 份中**没有 SSTI 独立报告**（`code-audit-cases.md` §八已如实标注），本篇靠打法与公开手法支撑 |
| `nosql-injection-test.md` | **NoSQL 注入（打深篇，主指 MongoDB）**（2026-09-15 补入）：`sqli-advanced-test.md` §7 有 NoSQL 操作符速查，本篇是专篇打深。**认后端六特征**（`MongoError` / 24 位 hex `ObjectId` / Node+Mongoose 等栈 / JSON 可嵌套不报 400 / PHP `?a[$ne]=` 数组语法 / ES 的 `search_phase_execution_exception`），按命中率排序的注入口（登录 > 搜索筛选 > 导出 > ID 查询 > token 校验 > 聚合）。**三步判定**：类型混淆（标量换对象看是否 500/unknown operator）→ 操作符生效（恒真条件做差分）→ 定利用面。含**操作符速查表**（`$ne $gt $regex $in $exists $or $where $expr $not $type $mod $function`，点明 `$gt` 对字符串是字典序，这是二分提数据的数学基础）+ 认证绕过（**第一件事是把登录请求改成 `application/json`**，成功率立涨）+ 数据提取（`$regex` 逐位爆破 / `$gt` 二分每字节约 7 次请求）+ `$where` 与 `$function` → RCE（含 `constructor.constructor('return process')()` VM 逃逸，真实案例见 `sqli-cases.md` §D YApi `/api/interface/up`）+ 时间盲注 + **PHP/Node/Python/Java/Go 驱动参数格式差异表**（Mongoose 严格 schema 会剥操作符）。**边界：27017 / 9200 / 6379 是未授权不是注入**，别混写。红线：`$where` 只跑 `id`，不做持久化不横向 |

| `subkb-dig-ideas-cases.md` | **ima 订阅库「挖洞思路总结」58 项沉淀**（2026-09-15，首个订阅库素材源，kb_id=7452645149118071）：全文精读 11 + 简介级 ≈40 + md 5 份 220030 读取失败。§1 逻辑漏洞 16 张速查卡（新增：支付回调劫持"只信参数不信主动查询"、业务流程绕过四步法、反向支付、邮箱资源永久占用）；§2 JS 逆向三案（Hawk 签名逆向→遍历 7 万身份证 / digest=MD5("/domain/url"+ts) / 通达 OA 默认密码+前端 RSA——**"先搜 config 再搜 key"**、XHR 断点定位法）；§3 EDU（隐藏接口三板斧"转子跑 reg 接口"、211 高校 操作手册→roleId 越权→roleIds:1 超管→18 万三要素→报表 SQL `UNION db_name()`→xp_cmdshell RCE 全链，含 fileProperty=UUID 白逆一天、xp_cmdshell 落在数据库主机两条踩坑）；§4 **AI 漏洞专章（技能首个 AI 类型素材）**：测绘语法 `domain:"x.edu.cn" AND (response:"hi..." and not domain:"hi...")`、判真假 AI（1+1 测试+问模型来源）、输入层敏感词 6 式（**语序打乱通过率最高**+AI 随机性多发碰撞）、返回层 3 式（show 开关/空字符阻断/`<thinking>` 包裹）、逐步降底线、过度信息获取、RCE 防幻觉判据（新开无记忆对话验证，企业 SRC 多要求 docker 逃逸）、公众号 AI 自动回复新入口。红线：色情诱导仅框架不收成品魔咒，政治类不挖 |

| `subkb-netsec-cases.md` | **ima 订阅库「网络安全知识库」1616 项定向开垦**（2026-09-15，第 2 个订阅库，kb_id=7304699371875661）。**不全量翻**（Nday/蓝队/培训文占比高），实读 5 篇 + 顶层 ≈30 项简介级。§1 **金融 SRC 场景全谱**：开户（传参是组件数组时**删掉手机组件仍可开户**、**后端查缓存导致一套证件开多户**、`request_id` 遍历拿全量三要素）、支付（并发提现无分布式锁、**负值反冲**、**int64 溢出**——Go 金额×精度超 9.22e18 高位截断且**不报错**、改 `xx_id=0` 免手续费）、优惠券（券码爆破按**响应长度**判存在、无锁版 `atomic.AddInt64` 可重复核销）、查询（**隐藏参数 fuzz**：后端"优先取 POST 的 userid、再解 token"而正常调用 POST 为空 → 拿返回包字段攒字典 fuzz 出 `uid`；**ToC/ToB 网关混用**：导管理端 a.com 的 Burp history 用普通 token 到 b.com 重放，XML→Base64 解码→CSV→Intruder **Pitchfork**）、资源存储（合同 ID 递增、Bucket Public Read）、SSRF（**域名白名单**下找可控跳转）、第三方（JWT 密钥硬编码、`curl http://password.xxx/db-prod-pass` 式运维后门）。§2 **Fastjson"不看版本看依赖"**：出网要分 DNS 与 LDAP 分别验；`{"@type":"java.lang.AutoCloseable"}` 触发报错直接吐版本；**依赖探测三招 DNS/延时/报错**，报错式 `{"@type":"java.lang.Character","@type":"java.lang.Class","val":"类名"}` —— 存在回显 `can not cast to char`、不存在正常回显，据此判 BCEL/CC/TemplatesImpl；CC 3.2.1+ 禁 `InvokerTransformer` 序列化（报错提示需 `enableUnsafeSerialization=true`）→ 转 **CB 链**；工具 Javachains + jmg 回显马。§3 **LLM 辅助审计工作流**（补 AI 专章的"第二种用法"）：Fiddler `Save All Sessions as Text` + **微信 4.1 后小程序包路径 `...\xwechat\radium\Applet\packages`** + `KillWxapkg -id= -in= --restore` → 搜 `getSignatureHeader(` 定位 `vendor.js` → LLM 还原签名（实例 `MD5("xt"+nonce+fixedUrl+timestamp+salt)`，含 phone 字段时参与签名）→ 报 IDOR 与"**非法图床**"类业务滥用风险；**Prompt 三段式**（角色设定 / 任务约束"严禁编造字段" / JSON 输出）；**两条红线：投喂前脱敏 Token-Cookie、AI 结论必须人工复现**。§4 **Electron 客户端 RCE**：Markdown 渲染 XSS → `process.binding('process_wrap').Process` → `spawn cmd.exe`（需 nodeIntegration 开启）。§5 未读清单（AI 挖 IoT RCE、SQLi 打法、JS 审计拿后台、某米摄像头逆向）与排除项（Nday 归 `nday-watchlist-2026.md`、蓝队情报不收）。红线：金融并发/负值只留判据不留批量脚本，签名只留算法结构不留真实 key |
| `subkb-airedteam-cases.md` | **ima 订阅库「AI红队指南攻防知识库」1474 项定向开垦**（2026-09-15，第 3 个订阅库，kb_id=7279055409794158）。顶层普查 50 + 「大模型」目录 60 全普查 + 精读 5 篇。§1 **MCP 服务未授权 → RCE（全新资产面）**：大量开源 MCP 无鉴权由 `uvicorn` 直挂公网；hunter `header.server=="uvicorn" && header.status_code=="404" && header="text/plain"`（实测 2789 资产/2437 IP，端口集中 8000·8001·443·60）→ 逐个访问 `/sse` 验 SSE 模式 → `mcp.ClientSession.list_tools()` **未授权即可列工具**，拿到 `python_code_execution`(RCE)/`mysql_execute_sql`·`execute_sql`(拖库)/`navigate_click`·`pdf_doc_ppt_reader`(SSRF+文件读)；利用端不用自己写客户端，Trae/Cursor 填 `{"mcpServers":{"test":{"url":"http://x/sse"}}}` 让 AI 直接调。§2 **MCP 协议攻击面 5 类**（腾讯朱雀体系）：TPA 工具投毒（描述注释里 `<IMPORTANT>` 藏指令 + Cursor 超长参数不换行 → 用户直接 Run；真实案例劫持 whatsapp-mcp 偷聊天记录）、Rug Pulls（远程静默改描述、无完整性校验）、**Shadowing 影子攻击（恶意 MCP 描述改写其他可信工具行为，用户根本不用调它）**、命令注入（MCP 自带执行/DB/文件能力 + 公网暴露；某交易所 MCP 对话直接转账）、供应链/越狱/API KEY 窃取；附协议 6 大缺陷与 A2A `/.well-known/agent.json` 能力泄露。§3 **ForcedLeak**（Salesforce Agentforce 9.4 分）：多步调用把提示注入**串成链**，污染上下文接力传递，每步单看合规 → 绕过权限与签名校验，被称"AI 时代的 XSS"；测 Agent **必须测多步链的上下文污染**。§4 **输入侧新手法**：**Unicode Tags Block（U+E0000–U+E007F）不可见字符走私**——镜像 ASCII、UI 不渲染但 tokenizer 能解析，可藏注入 / 让 LLM 反向输出外带 / **利用 Human-in-the-Loop 诱人工复制转发批准**，工具 ASCII Smuggler（此手法不止打 LLM，凡"渲染层与解析层不一致"都可用）；字体投毒（自定义字体+CSS 制造渲染与解析差异）；**攻击增强三分类**（编码类 Base64/LeetSpeak/ROT13/HTML实体/URL/Unicode/十六进制，单次类 提示注入·灰盒·数学问题·多语言(**低资源语种如巴斯克语/威尔士语/斯瓦希里语是盲区**)·DAN·角色扮演，对话类 迭代·树形·多轮 Crescendo·PAIR——**对话类最有效**），方法论要点是**基线攻击故意写弱**以绕过输入过滤再靠增强放大。§5 **LLM 红队维度**：五大风险类别（伦理/非法活动/品牌/**数据隐私 PII·DB·API key**/**未授权访问 Debug·BOLA·RBAC·BFLA·SSRF**——后两类才是 SRC 能变现的，把 AI 应用当 Web 打只是入口换成对话）+ OWASP LLM Top10 2025 编号（写报告对编号更好定级）+ 工具 Garak/DeepTeam/DeepEval/CyberSecEval/FuzzyAI。§6 **Java 写文件→RCE：SO 劫持**（补"落地"空白）：`System.loadLibrary` 先查 sys_paths 再查 usr_paths（Linux `/usr/java/packages/lib`·`/usr/lib/x86_64-linux-gnu/jni`·`/lib`·`/usr/lib`…，Windows `%PATH%`）；**三条铁律**——已加载的库改写再触发会 SIGBUS 崩溃、`cat /proc/<pid>/maps` 枚举已加载、**每个库只有一次机会**；首选 `libawt.so`（SpringBoot 不加载图形类，`java.awt`/`javax.swing`/`JMenuItem` 触发）；**so→触发类对照表 14 项**（libfontmanager/libprefs/libjavajpeg/librmi/libunpack/libj2pkcs11/libjsound/libsctp/libattach/libmanagement_agent…）；触发用 `Class.forName(true)` 打静态块；**JNI 优雅版**重写 `JPEGImageDecoder#initIDs` 在 C 里 Base64→defineClass→newInstance，不崩不阻塞（constructor 版弹 shell 会阻塞 web）；第三方 ojdbc `heteroxa21`/`ocijdbc21`；**JDK 路径未知时**用 fastjson DNS 链（`java.net.Inet4Address` → `libnss_dns.so.2`/`libresolv.so.2`）或内置 awt 白名单（`java.awt.Font` → libpng16/libfreetype/libawt/libfontmanager，`Rectangle`/`Color` → liblcms）。§7 合并去向 + §8 剩余可挖清单（18 个目录 folder_id + Vue JS 收集/阿里V2滑块/k8s RBAC/ingress 后门/DCOM 横向 等待读；腾讯沙龙 6 份 PDF `can_fetch_content:false` 需客户端手动开）。红线：越狱成品 payload 不收、色情诱导只留框架、政治类不挖 |

**合计：89 个知识文件**（不含本 README；`_work/` 为中间产物目录不计入）。其中 48 篇为原库，14 篇为 2026-09-13 补入：`sqli-advanced-test.md`、`password-reset-test.md`、`middleware-unauth-test.md`、`postmessage-dom-test.md`、`ratelimit-abuse-test.md`、`artifact-intel-guide.md`、`redirect-ear-unauth.md`、`recon-fingerprint-cdn-wildcard.md`、`edge-asset-hunting.md`、`login-bypass-playbook.md`、`nday-watchlist-2026.md`、`captcha-bypass-test.md`、`biometric-bypass-test.md`、`src-case-study.md`——这 12 篇为**正文**，不是「收成一行」。另同日扩充了 `js-reverse-guide.md`（加 sourcemap / 硬编码凭据正则 / 影子 API 三块）、`idor-test.md`（越权+未授权系统化）、`logic-test.md`（数值矩阵/支付签名/场景发散）、`password-reset-test.md`（响应包状态值/接收端污染）。SRC 报告版式不在本库：见 `rules/vuln-report-format.md`。定级只认 format，本库不定级。另 `src-platform-and-search.md` 为 **2026-09-14 补入**（SRC 平台目录 + 搜索技巧），`ima-case-corpus.md` 为同日补入（ima 案例库 1300+ 份实战报告的去重提炼，原文远程在 ima `src` 知识库，本地只留打法浓缩），`logic-web-cases.md` 同为 **2026-09-14 补入**（逻辑漏洞 Web 195 条去重后 88 份唯一报告全量精读；中间产物在 `_work/`：`raw-list-195.md` 原始清单、`unique-list.md` 去重清单、`deep-1~6.md` 分批判读稿），`edusrc-cases.md` 同日补入（EduSRC 教育专项打底文件，内含逻辑漏洞 EduSRC 27 条去重后 21 份唯一文件全量精读；中间产物 `_work/unique-list-edusrc.md` + `deep-edusrc-a~c.md`。**至此逻辑漏洞类 222 份全部闭环**），`idor-cases.md` 同为 **2026-09-14 补入**（越权目录 168 条去重后 112 份精读：Web 70 → 本文件，EduSRC 42 → 并入 `edusrc-cases.md` §2.8；中间产物 `_work/unique-list-idor.md` + `deep-idor-a~f.md`。**至此越权类 168 份全部闭环**），`other-census.md` 同为 **2026-09-14 补入**（P1-4「其他」762 份构成调研结论：不是漏洞类型而是未归位附件倾倒口，去重后 ≈562 份，建议三分法处理；原始清单在 `_work/other-web-census.md` + `_work/other-edu-app-census.md`），`attack-chain-cases.md` 同日补入（**第 2a 批首件**：「其他/Web」实战攻防类 36 份完整攻击链复盘，35/36 读取成功，按"打点→上线→提权→横向→域/云"六阶段重组；含上传绕 WAF 全景表、国产 OA Nday 速查、不出网/站库分离落地、EDR 二次认证绕过、ToDesk config.ini 取密、etcd→Pod 逃逸、云 AK/SK 接管；**§0 红线**标注社工/钓鱼/免杀仅作认知；中间产物 `_work/unique-list-chain.md` + `deep-chain-a~c.md`）。2026-09-14 同日又补入 **13 篇**：`sqli-cases.md`、`xss-cases.md`、`infoleak-cases.md`、`cmd-injection-cases.md`、`csrf-cases.md`、`file-upload-cases.md`、`weak-password-cases.md`、`ssrf-cases.md`、`lfi-xxe-cases.md`（**P2 十类打包**：ima `src报告/` 下十个漏洞类型目录共 313 份，去重后精读约 160 份，每类一个文件，按"打法/泄露源/防护层级"重组而非按报告罗列；教育侧片段稿在 `_work/edusrc-*-add.md`，已并入 `edusrc-cases.md` §2.9~§2.13）+ `code-audit-cases.md`、`vendor-system-cases.md`（**2a 批**：「其他/Web」可挖原料，前者 115 条→41 份唯一→精读 23 份，后者 131 条交叉查重后只取新案例，按产品组织"认指纹→知道打什么"）+ `mobile-cases.md`（**P3 移动端专项**：28 份全读，抓包链路 / 小程序逆向三件套 / 加固对抗 / 客户端校验绕过 / 云资产接管）+ `archive-inventory.md`（**2b/2c**：归档清单与副本删除对照，不精读只留档）。另同日 `src-platform-and-search.md` 追加了「SRC 挖洞方法论精选」章节（来自「其他/Web」57 条方法论 → 精读 15~20 份），`waf-bypass.md` 补了 §2.8.1 上传文件名与头字段实战绕过。**至此 ima 案例库 1604 份中，逻辑漏洞 222 + 越权 168 + 十个漏洞类型 313 + 「其他」可挖原料与归档 ≈500 全部处置完毕，累计深挖 ≈1200 份**。2026-09-15 又补入 **2 篇**（不依赖 ima 新报告，属技能自身补全）：`ssti-test.md`（服务端模板注入打深）、`nosql-injection-test.md`（NoSQL/MongoDB 注入打深）——这两个类型是体检时发现的**唯一实质知识空洞**（此前全库只有零散提及，无成篇手册）。同日另修 `SKILL.md` 目录表：**12 个知识库文件此前未被路由**（`open-redirect-test.md`、`ghost-bits-cast-test.md`、`el-injection-test.md`、`insecure-scm-test.md` 4 篇成篇漏列 + 8 张指针卡片打包 2 行），已全部补入；并**批量给 29 个成篇手册补上「实战案例（双向引用）」指针**（覆盖率 21/64 → 50/64，剩余 14 个为「几乎不交」的指针卡片，故意不加）。
